print.htbHd <- function(x, ...) { str(x, give.attr = FALSE) }
print.htbDb <- function(x, ...) { str(x, give.attr = FALSE) }
print.htbRas <- function(x, ...) { str(x$da, give.attr = FALSE) }
print.htbHis <- function(x, ...) { str(x$da, give.attr = FALSE) }

